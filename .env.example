# TMDB credentials
TMDB_API_KEY=your_tmdb_api_key_here

# Firebase Web App Credentials
# Create a web app in Firebase Console, copy the config object values and put them in js/firebase-config.js
# You can use this template for local setup
